{
	"name": "ph-ack",
	"version": "1.0.0",
	"description": "Ph! Ack! is a really small package to debug process memory via Node.js.",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prepare": "npx tsc"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"ffi-napi": "^4.0.3"
	},
	"devDependencies": {
		"@types/ffi-napi": "^4.0.4",
		"@types/node": "^16.11.7",
		"typescript": "^4.4.4"
	},
	"files": [
		"dist",
		"README.md"
	]
}
